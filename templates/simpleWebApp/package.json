{
  "name": "{{projectName}}",
  "version": "{{projectVersion}}",
  "scripts": {
    "postinstall": "npm dedupe",
    "test": "mocha"
  },
  "dependencies": {
  },
  "devDependencies": {
    "chai": "^4.1.0",
    "mocha": "^3.4.2"
  }
}
