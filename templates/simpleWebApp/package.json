{
  "name": "{{projectName}}",
  "version": "{{projectVersion}}",
  "scripts": {
    "postinstall": "npm dedupe",
    "test": "mocha",
    "deploy": "webfunc deploy",
    "logs": "webfunc logs"
  },
  "dependencies": {
    "webfunc": "^0.1.0-alpha"
  },
  "devDependencies": {
    "chai": "^4.1.0",
    "mocha": "^3.4.2"
  }
}
